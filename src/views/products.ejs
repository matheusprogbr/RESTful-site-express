<!DOCTYPE html>
<html lang="pt-BR">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Products - matheusprogbr</title>

  <link rel="stylesheet" href="style.css">
</head>

<body>

  <header class="header">
    <div class="logo">
      <h1 class="title">E-commerce</h1>
    </div>
  </header>
  <table class="table">
    <thead>
      <tr>
        <th>Produto</th>
        <th>Preço</th>
        <th>&nbsp;</th>
      </tr>
    <tbody>
      <% for(product of products){ %>
      <tr>
        <td><%= product.attributes.name %></td>
        <td><%= product.attributes.price %> </td>
        <td><a class="delete-btn" href="http://localhost:3500<%=product.links.self%>">Excluir</a></td>
      </tr>
      <% } %>

    </tbody>
    </thead>
  </table>

  <script src="script.js"></script>
</body>

</html>